machine:
  node:
    version: 5.5.0

dependencies:
  pre:
    - npm install -g bower
  override:
    - bundle install
    - npm install
    - bower install

test:
  pre:
    - npm run build

deployment:
  mandrill:
    branch: doeg-deploy
    commands:
      - npm run publish
