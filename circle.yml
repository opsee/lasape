machine:
  node:
    version: 5.5.0

dependencies:
  override:
    - bundle install
    - npm install --verbose

test:
  pre:
    - npm run build

deployment:
  mandrill:
    branch: doeg-deploy
    commands:
      - npm run publish
